{
  "name": "music_sheet",
  "version": "1.0.0",
  "description": "This project is bootstrapped with [Create Elm App](https://github.com/halfzebra/create-elm-app).",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "elm-app start",
    "css:watch": "npx tailwind -w -i src/input.css -o src/main.css",
    "css:build": "npx tailwind -i src/input.css -o src/main.css",
    "build": "npm run css:build && elm-app build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.1"
  }
}
